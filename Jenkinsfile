// Transitional entry point: jenkins/projects.yaml in ci-cd-platform still
// points script_path here. The canonical contract is .versatushpc/Jenkinsfile;
// remove this file once the platform-side script_path switch is applied.
@Library('versatus-ci-cd') _
versatusPipeline()
